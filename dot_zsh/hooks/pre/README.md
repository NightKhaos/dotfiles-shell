# Pre-hooks

Sourced at the TOP of `~/.zshrc`, before completion setup and other config.

Use for tools that must inject early in the shell initialisation sequence.

Example: kiro-cli pre-hook (`10-kiro-cli.rc`)
