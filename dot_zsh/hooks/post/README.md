# Post-hooks

Sourced at the BOTTOM of `~/.zshrc`, after all other config including prompt setup.

Use for tools that must inject late in the shell initialisation sequence.

Example: kiro-cli post-hook (`10-kiro-cli.rc`)
